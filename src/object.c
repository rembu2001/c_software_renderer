/**
 * Accepts the name of an object file from the `mesh` directory
 * and 'creates' the object in space about the origin as an array
 * of triangles with vertices and normal vectors.
 */
// OBJECT* create_object(char* object_name);
//   Create the object on the heap

/**
 * Computes the normal vector of a triangle given it's vertices
 * (need to figure out how to decide which way it goes...)
 */